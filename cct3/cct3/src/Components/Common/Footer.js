import { IDSFooter } from '@inera/ids-react';

function Footer() {

    return(
        <IDSFooter class="ids-hide ids-show-inera-admin footer ids-mt-10" type="inera-admin" headline="CertCenter">
        <p>
          En tjänst från Inera Test & utveckling AB
        </p>
      </IDSFooter>
    )
}

export default Footer;